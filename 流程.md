# L4D2Fix DXVK + L4N 处理流程

本文档描述当前 Go + Win32 便携工具的实际处理流程。主程序入口为：

```text
L4N_Go_Win32_Portable/L4N_Go_Win32.exe
```

> README 面向使用者；本文档面向维护和排查，重点记录程序的实际执行顺序、文件位置和边界规则。

## 目录

- [1. 便携目录结构](#1-便携目录结构)
- [2. 主程序 UI](#2-主程序-ui)
- [3. 独立字体工具](#3-独立字体工具)
- [4. 一键处理流程](#4-一键处理流程)
- [5. 一键清理流程](#5-一键清理流程)
- [6. MOD 备份与恢复流程](#6-mod-备份与恢复流程)
- [7. 验证方式](#7-验证方式)
- [8. Git 与发布注意事项](#8-git-与发布注意事项)

## 1. 便携目录结构

发布版是非单文件便携版。exe 放在便携目录根部，其他运行资源统一放在 `resources/`：

```text
L4N_Go_Win32_Portable/
  L4N_Go_Win32.exe
  L4N_Font_Change.exe
  L4D2_Font_Change.exe
  resources/
    VC_redist.x86.exe
    VC_redist.x64.exe
    启动项指令【四】.txt
    验证指令【六】.txt
    tools/
      Everything/
    L4N_dxvk2.7.1/
    L4N_dxvk2.3.1_AMD/
    addons_backup/              # 运行“备份MOD”后生成
    display_settings_backup/    # 运行“备份MOD”后生成
```

目录约定：

| 约定 | 说明 |
| --- | --- |
| `resources/` | 不应改名或移动。程序会优先从 exe 同级的 `resources/` 读取补丁、运行库和说明文件。 |
| 字体工具 | `L4N_Font_Change.exe` 和 `L4D2_Font_Change.exe` 放在便携目录根部。 |
| 发布包 | 当前发布习惯是不自动打包 zip，需要时再手动打包。 |

## 2. 主程序 UI

UI 上的六个按钮按三列两行排列：

```text
通用处理    备份MOD    一键清理
AMD处理     恢复MOD    系统字体/游戏默认
```

| 按钮 | 处理内容 |
| --- | --- |
| `通用处理` | 使用 `resources/L4N_dxvk2.7.1/` 作为补丁源目录。 |
| `AMD处理` | 使用 `resources/L4N_dxvk2.3.1_AMD/` 作为补丁源目录。 |
| `备份MOD` | 复制游戏 `left4dead2/addons/` 下的全部内容到 `resources/addons_backup/`，保持目录和文件结构；同时备份显示设置 `left4dead2/cfg/video.txt`。 |
| `恢复MOD` | 复制 `resources/addons_backup/` 下的全部内容回游戏 `left4dead2/addons/`，保持目录和文件结构；遇到同名文件会覆盖；同时恢复显示设置 `left4dead2/cfg/video.txt`。 |
| `一键清理` | 读取 exe 同级 `.l4n_auto_backup/manifest.json`，恢复游戏文件和 Steam 配置。 |
| `系统字体/游戏默认` | 通用处理后切换 `left4dead2/neko/config.vdf` 中的 `font` 配置块。 |

建议在点击处理或清理前关闭 Steam 和游戏，避免 Steam 退出时覆盖配置文件。

## 3. 独立字体工具

| 程序 | 使用场景 | 核心行为 |
| --- | --- | --- |
| `L4N_Font_Change.exe` | 游戏使用 L4N 平台。 | 修改 `left4dead2/neko/config.vdf` 的 `font` 配置块。 |
| `L4D2_Font_Change.exe` | 旧的 `Font_change` 文件复制方式。 | 修改 `FontMod.yaml` 并复制或删除游戏根目录中的同名文件。 |

### 3.1 L4N_Font_Change.exe

用于 L4N 平台字体切换。工具会自动定位 L4D2 游戏目录，并修改：

```text
<L4D2游戏根目录>/left4dead2/neko/config.vdf
```

行为说明：

| 操作 | 执行结果 |
| --- | --- |
| 下拉框选择字体 | 更新当前选择和右侧预览。 |
| `更换字体` | 修改 `font` 配置块中生效的 `"Tahoma" "字体名"` 行。 |
| `恢复默认字体` | 注释整个 `font` 配置块。 |
| `浏览字体文件` | 只执行自动安装字体流程，不直接修改 `config.vdf`。 |

> 字体文件安装后，仍需要在下拉框中确认字体，再点击 `更换字体`。如果 `config.vdf` 不存在，说明没有进行通用处理，工具会提示先进行通用处理。

L4N 字体配置注释规则：

- 工具只会启用或修改真正生效的 `"Tahoma" "字体名"` 替换行。
- 配置原文中已经是注释状态的说明、示例和备用字体行必须保持注释状态，例如 `//"Tahoma" "微软雅黑"`、`// "HUD使用的字体名" "微软雅黑"`、`// "Stubble bold" "微软雅黑"`。
- 恢复默认字体时，程序会给整个 `font` 配置块增加一层 `//` 注释。
- 再次启用字体时，程序只移除恢复默认时添加的最外层注释，不会把配置原文中本来注释的行误激活。

### 3.2 L4D2_Font_Change.exe

用于旧的 `Font_change` 文件复制方式。此方法在游戏使用 L4N 平台时无效。

行为说明：

| 操作 | 执行结果 |
| --- | --- |
| 下拉框选择字体 | 更新当前选择和右侧预览。 |
| `更换字体` | 修改 `Font_change/FontMod.yaml` 中的 `fonts.Tahoma.name`，随后复制 `Font_change` 目录内的全部文件到 L4D2 游戏根目录。 |
| `恢复默认字体` | 扫描 `Font_change` 目录内的文件，只删除游戏目录中同相对路径的文件。 |
| `浏览字体文件` | 自动安装字体文件。安装后仍需在下拉框中确认字体，再点击 `更换字体`。 |

`Font_change/fonts/` 内的字体文件是可选资源，不再是便携包运行条件；默认配置使用系统常见字体 `Microsoft YaHei`。

> 恢复默认字体不会删除项目内的 `Font_change` 文件，只会处理游戏目录中的同名目标文件。

字体选择规则：

- 下拉框列出当前系统已安装字体。
- 支持手动输入字体名，并自动匹配已安装字体。
- 输入不存在的字体名时，工具会阻止继续更换。

## 4. 一键处理流程

### 4.1 安装运行库

程序依次尝试运行：

```text
VC_redist.x86.exe /install /quiet /norestart
VC_redist.x64.exe /install /quiet /norestart
```

运行库安装器放在：

```text
resources/VC_redist.x86.exe
resources/VC_redist.x64.exe
```

安装器返回已安装、需重启或不支持时，程序会记录日志并继续后续步骤。单个安装器最多等待 5 分钟，超时后跳过并继续。

### 4.2 定位 L4D2 游戏目录

程序会按以下顺序查找真实的 `left4dead2.exe`：

1. 读取 Steam 注册表路径。
2. 读取 Steam `libraryfolders.vdf`。
3. 查找 AppID `550` 的 `appmanifest_550.acf`。
4. 根据 `installdir` 定位 `steamapps/common/<installdir>/left4dead2.exe`。
5. 如果 Steam 路径定位失败，调用 `resources/tools/Everything/es.exe` 搜索。
6. 如果 Everything 不可用，再扫描常见 SteamLibrary 路径。

程序会排除本项目和便携目录内的 `left4dead2.exe`，避免误把补丁包里的文件当作游戏本体。

真实游戏目录需要满足以下条件之一：

- `left4dead2/pak01_dir.vpk` 存在
- `left4dead2/gameinfo.txt` 存在
- 路径匹配 Steam 的 `steamapps/common/Left 4 Dead 2/left4dead2.exe`

### 4.3 备份并覆盖游戏源文件

补丁目录中的所有文件都会按相对路径覆盖到游戏根目录。

示例：

```text
resources/L4N_dxvk2.7.1/dxgi.dll
```

会覆盖到：

```text
<L4D2游戏根目录>/dxgi.dll
```

```text
resources/L4N_dxvk2.7.1/bin/left4neko.dll
```

会覆盖到：

```text
<L4D2游戏根目录>/bin/left4neko.dll
```

覆盖前，程序会为每个目标文件记录：

- 游戏目标文件路径
- 相对路径
- 原文件是否存在
- 备份文件路径
- 原文件权限
- 原文件修改时间

备份和记录保存在 exe 同级：

```text
L4N_Go_Win32_Portable/.l4n_auto_backup/
```

其中：

```text
.l4n_auto_backup/manifest.json
.l4n_auto_backup/files/
.l4n_auto_backup/steam/
```

`manifest.json` 会在每个文件首次备份记录后立即保存，并使用临时文件替换方式写入，降低中途退出导致清单损坏的风险。

重复执行处理时，如果目标文件已经在 manifest 中有记录，程序不会覆盖最初备份，避免备份被补丁文件污染。

### 4.4 写入 Steam 启动项

程序会修改 Steam 用户配置中的 AppID `550`，写入：

```text
-heapsize 2097152 -processheap -high -novid -nojoy -steam -lv -vulkan
```

配置来源优先读取：

```text
resources/启动项指令【四】.txt
```

如果文件不存在或内容无效，则使用程序内置默认值。

Steam 配置文件修改前会备份到：

```text
.l4n_auto_backup/steam/
```

如果 Steam 正在运行，程序会提示用户：Steam 退出时可能覆盖配置，必要时关闭 Steam 后重新执行。

## 5. 一键清理流程

点击 `一键清理` 后，程序读取：

```text
L4N_Go_Win32_Portable/.l4n_auto_backup/manifest.json
```

然后按清单执行：

1. 对原本存在的文件：从 `.l4n_auto_backup/files/` 复制回原路径。
2. 对原本不存在、由补丁新增的文件：删除该文件。
3. 删除新增文件后，如果父目录为空，则清理空目录，直到游戏根目录为止。
4. 恢复文件权限和修改时间。
5. 从 `.l4n_auto_backup/steam/` 恢复 Steam 配置。
6. 清理完成后删除 `.l4n_auto_backup/`。

如果 `.l4n_auto_backup/manifest.json` 不存在，说明没有可用备份，清理会失败并在日志中提示。

## 6. MOD 备份与恢复流程

L4D2 的 MOD 目录为：

```text
<L4D2游戏根目录>/left4dead2/addons/
```

### 6.1 备份 MOD

点击 `备份MOD` 后，程序会：

1. 自动定位 L4D2 游戏目录。
2. 找到 `<L4D2游戏根目录>/left4dead2/addons/`。
3. 清空旧的 `resources/addons_backup/`。
4. 将 `addons/` 下的全部目录和文件复制到 `resources/addons_backup/`。
5. 复制时保持 `addons/` 内原有目录结构和文件相对路径。
6. 将显示设置 `left4dead2/cfg/video.txt` 复制到 `resources/display_settings_backup/video.txt`。

备份目标为：

```text
L4N_Go_Win32_Portable/resources/addons_backup/
```

显示设置备份目标为：

```text
L4N_Go_Win32_Portable/resources/display_settings_backup/video.txt
```

### 6.2 恢复 MOD

点击 `恢复MOD` 后，程序会：

1. 自动定位 L4D2 游戏目录。
2. 读取 `resources/addons_backup/`。
3. 将备份目录下的全部内容复制回 `<L4D2游戏根目录>/left4dead2/addons/`。
4. 复制时保持备份中的目录结构和文件相对路径。
5. 如果 `resources/display_settings_backup/video.txt` 存在，则复制回 `<L4D2游戏根目录>/left4dead2/cfg/video.txt`。

恢复过程只复制备份文件回 `addons/`，遇到同名文件会覆盖；不会主动删除 `addons/` 中其他未包含在备份里的文件。

## 7. 验证方式

进入游戏后打开控制台。

输入：

```text
mat_info
```

期望看到：

```text
ShaderAPI: shaderapivk
```

再输入：

```text
mem_dump
```

期望看到：

```text
2,048.00MB
```

## 8. Git 与发布注意事项

Git 仓库只保存源码、配置和小体积资源。以下内容不进入 Git：

- 便携版目录 `L4N_Go_Win32_Portable/`
- 运行库安装器 `VC_redist*.exe`
- 补丁目录和补丁压缩包
- 生成的 exe、dll、vcs、zip、7z、syso
- Everything 工具目录
- Go 缓存、临时文件、日志和备份目录

便携包和大体积补丁资源应通过 GitHub Release 附件分发，而不是提交到 Git 历史。
